services:
  lintic:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      LINTIC_API_KEY: ${LINTIC_API_KEY}
      LINTIC_ADMIN_KEY: ${LINTIC_ADMIN_KEY}
      LINTIC_SECRET_KEY: ${LINTIC_SECRET_KEY}
      LINTIC_DATABASE_PATH: /app/data/lintic.db
    volumes:
      - ./lintic.yml:/app/lintic.yml:ro
      - ./data:/app/data
